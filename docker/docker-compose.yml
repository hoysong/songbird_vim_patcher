services:
  cluster-tool:
    build: .
    container_name: cluster-tool
    volumes:
      - server:/root/home
    stdin_open: true
    tty:  true
volumes:
  server:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/seongkim/work
